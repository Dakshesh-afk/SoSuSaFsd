@page "/category/{Id:int}"
@rendermode InteractiveServer
@using SoSuSaFsd.Data
@using SoSuSaFsd.Domain
@using Microsoft.EntityFrameworkCore
@using Microsoft.AspNetCore.Components.Authorization
@using Microsoft.AspNetCore.Identity
@using Microsoft.AspNetCore.Hosting
@inject IDbContextFactory<SoSuSaFsdContext> DbFactory
@inject NavigationManager Navigation
@inject AuthenticationStateProvider AuthStateProvider
@inject IServiceScopeFactory ScopeFactory
@inject IWebHostEnvironment Environment

<PageTitle>@Title</PageTitle>
<link rel="stylesheet" href="/styles/category-details.css" />

<header class="site-header">
    <div class="header-container">
        <div class="logo-area" onclick="location.href='/'">
            <div class="logo-icon"><i class="fas fa-check"></i></div>
            <span class="logo-text">SoSuSa</span>
        </div>

        <div style="display: flex; gap: 10px; align-items: center;">
            @* --- NEW REFRESH BUTTON --- *@
            <button class="login-btn secondary" style="min-width: 40px; padding: 0 10px;" title="Refresh Page" @onclick="RefreshPage">
                <i class="fas fa-redo"></i>
            </button>

            <button class="login-btn" onclick="location.href='/'">Back Home</button>
        </div>
    </div>
</header>

<div class="main-layout">
    @* --- LEFT SIDEBAR --- *@
    <aside class="sidebar sidebar-left">
        <div class="sidebar-section scrollable-list">
            <h3>Following</h3>
            <ul class="category-list">
                @if (FollowedCategories != null && FollowedCategories.Any())
                {
                    @foreach (var cat in FollowedCategories)
                    {
                        <li>
                            <a class="category-link" href="#" @onclick:preventDefault @onclick="@(() => NavigateToCategory(cat.Id))">
                                <span class="link-text-wrapper"><span class="hashtag">#</span> @cat.CategoryName</span>
                                @if (cat.IsVerified)
                                {
                                    <i class="fas fa-check-circle verified-badge-small" title="Verified"></i>
                                }
                            </a>
                        </li>
                    }
                }
                else
                {
                    <AuthorizeView>
                        <Authorized>
                            <li class="empty-list-item">You aren't following anything yet.</li>
                        </Authorized>
                        <NotAuthorized>
                            <li class="empty-list-item">Login to see follows.</li>
                        </NotAuthorized>
                    </AuthorizeView>
                }
            </ul>
        </div>

        <div class="sidebar-footer">
            <div class="category-search-container">
                <input type="text" placeholder="Find category..." @bind="searchTerm" @bind:event="oninput" />
                <button type="button" class="create-cat-btn" title="Create New Category" @onclick="HandleCategorySearch">
                    <i class="fas fa-plus"></i>
                </button>
            </div>
        </div>
    </aside>

    @* --- MAIN FEED --- *@
    <main class="feed-area">
        @if (CurrentCategory == null)
        {
            <div class="loading-state">
                @if (IsLoading)
                {
                    <span>Loading...</span>
                }
                else
                {
                    <span>Category not found.</span>
                }
            </div>
        }
        else
        {
            <div class="category-header-card">
                <h1 class="page-title">
                    #@CurrentCategory.CategoryName
                    @if (CurrentCategory.IsVerified)
                    {
                        <i class="fas fa-check-circle verified-badge" title="Verified Category"></i>
                    }
                </h1>
                <p class="cat-desc">@(string.IsNullOrEmpty(CurrentCategory.CategoryDescription) ? "No description provided." : CurrentCategory.CategoryDescription)</p>

                @if (!string.IsNullOrEmpty(errorMessage))
                {
                    <div class="alert alert-error">
                        <strong>Error:</strong> @errorMessage
                    </div>
                }

                <div class="action-buttons">
                    <button class="follow-btn @(isFollowing ? "following" : "not-following")" @onclick="ToggleFollow">
                        @(isFollowing ? "Unfollow" : "Follow")
                    </button>
                    @if (CanUserPost())
                    {
                        <button class="login-btn" @onclick="() => showPostModal = true">Create Post</button>
                    }
                    else if (hasPendingRequest)
                    {
                        <button class="login-btn" disabled>Request Pending</button>
                    }
                    else if (CurrentCategory?.IsVerified == true)
                    {
                        <button class="login-btn" @onclick="() => showRequestModal = true">Request Access</button>
                    }
                    else
                    {
                        <button class="login-btn" @onclick="() => showPostModal = true">Create Post</button>
                    }
                </div>

                @if (CurrentCategory?.IsVerified == true && !CanUserPost() && !hasPendingRequest)
                {
                    <div class="alert alert-warning">
                        <strong>⚠️ Verified Category:</strong> This category is restricted. You must request access to post here.
                    </div>
                }
                else if (hasPendingRequest)
                {
                    <div class="alert alert-info">
                        <strong>ℹ️ Pending:</strong> Your request to post in this verified category is awaiting admin approval.
                    </div>
                }
            </div>

            <hr />

            @if (CategoryPosts != null && CategoryPosts.Any())
            {
                <div style="margin-bottom: 20px;">
                    <input type="text" placeholder="Search posts in this category..." @bind="postSearchTerm" @bind:event="oninput" style="width: 100%; padding: 12px; border: 1px solid #ddd; border-radius: 8px; font-size: 14px; box-sizing: border-box;" />
                </div>

                // Calculate filtered posts
                var resultList = (IEnumerable<Posts>)CategoryPosts;

                // 1. If user typed in the local search bar, prioritize that
                if (!string.IsNullOrWhiteSpace(postSearchTerm))
                {
                    resultList = resultList.Where(p =>
                    p.Content.Contains(postSearchTerm, StringComparison.OrdinalIgnoreCase) ||
                    (p.User?.UserName ?? "").Contains(postSearchTerm, StringComparison.OrdinalIgnoreCase));
                }
                // 2. Otherwise, if we came from the main search (URL has focusPostId), show only that post
                else if (FocusPostId.HasValue)
                {
                    resultList = resultList.Where(p => p.Id == FocusPostId.Value);
                }

                var filteredPosts = resultList.ToList();

                @* "View All" button if looking at specific search result *@
                @if (FocusPostId.HasValue && string.IsNullOrWhiteSpace(postSearchTerm))
                {
                    <div style="margin-bottom: 15px;">
                        <button class="login-btn secondary" @onclick="ClearSearchAndNavigate">
                            ← View all posts in #@CurrentCategory?.CategoryName
                        </button>
                    </div>
                }

                @if (filteredPosts.Any())
                {
                    @foreach (var post in filteredPosts)
                    {
                        <div class="post-card">
                            <div class="post-header">
                                <div class="post-avatar" style="background-image: url('@(post.User?.ProfileImage ?? "")');"></div>
                                <div>
                                    <b>@(post.User?.UserName ?? "User")</b>
                                    <div class="post-date">@post.DateCreated.ToString("MMM dd, yyyy • HH:mm")</div>
                                </div>
                            </div>
                            <div class="post-content">@post.Content</div>
                            @if (post.Media != null && post.Media.Any())
                            {
                                @RenderCarousel(post)
                            }
                            <div class="post-actions">
                                <button class="action-btn" @onclick="() => ToggleLike(post.Id)">
                                    @RenderLikeButton(post)
                                </button>
                                <button class="action-btn" @onclick="() => ToggleComments(post.Id)">
                                    <i class="far fa-comment"></i> Comment
                                </button>
                                <button class="action-btn" @onclick="@(() => OpenReportModal(post.Id))" title="Report Post">
                                    <i class="far fa-flag"></i> Report
                                </button>
                            </div>
                            @if (IsCommentSectionVisible(post.Id))
                            {
                                @RenderCommentSection(post)
                            }
                        </div>
                    }
                }
                else
                {
                    <div class="empty-state center">
                        No posts found matching "<b>@postSearchTerm</b>"
                    </div>
                }
            }
            else
            {
                <div class="empty-state center">
                    No posts yet. Be the first to post in <b>#@CurrentCategory.CategoryName</b>!
                </div>
            }
        }
    </main>

    @* --- RIGHT SIDEBAR --- *@
    <aside class="sidebar sidebar-right">
        <div class="sidebar-section">
            <h3>Verified Categories</h3>
            <ul class="category-list">
                @if (VerifiedCategories != null)
                {
                    @foreach (var cat in VerifiedCategories)
                    {
                        <li>
                            <a class="category-link" href="#" @onclick:preventDefault @onclick="@(() => NavigateToCategory(cat.Id))">
                                <span class="link-text-wrapper"><span class="hashtag">#</span> @cat.CategoryName</span>
                                <i class="fas fa-check-circle verified-badge-small" title="Verified"></i>
                            </a>
                        </li>
                    }
                }
            </ul>
        </div>
        <div class="sidebar-section">
            <h3>Recently Created</h3>
            <ul class="category-list">
                @if (RecentCategories != null && RecentCategories.Any())
                {
                    @foreach (var cat in RecentCategories)
                    {
                        <li>
                            <a class="category-link" href="#" @onclick:preventDefault @onclick="@(() => NavigateToCategory(cat.Id))">
                                <span class="link-text-wrapper"><span class="hashtag">#</span> @cat.CategoryName</span>
                                @if (cat.IsVerified)
                                {
                                    <i class="fas fa-check-circle verified-badge-small" title="Verified"></i>
                                }
                            </a>
                        </li>
                    }
                }
                else
                {
                    <AuthorizeView>
                        <Authorized><li class="empty-list-item">You haven't created any categories yet.</li></Authorized>
                        <NotAuthorized><li class="empty-list-item">Login to see your categories.</li></NotAuthorized>
                    </AuthorizeView>
                }
            </ul>
        </div>
    </aside>
</div>

@* --- MODALS --- *@

@* Post Modal *@
@if (showPostModal)
{
    <div class="modal">
        <div class="modal-content">
            <span class="modal-close" @onclick="ClosePostModal">×</span>
            <h2>Create Post</h2>
            <div class="modal-subtitle">Posting to <b>#@CurrentCategory?.CategoryName</b></div>
            <textarea class="modal-textarea" placeholder="What's on your mind?" @bind="newPostContent"></textarea>
            <div class="file-input-wrapper">
                <label>Add Media (Images or Videos): @if (isUploading) {
                <span class="upload-loading">Uploading... please wait.</span>
            }
</label>
            <InputFile OnChange="HandleFilesSelected" multiple accept=".jpg,.jpeg,.png,.gif,.mp4,.webm,.mov" />
        </div>
        <div class="preview-container">
            @for (int i = 0; i < pendingUploads.Count; i++)
            {
                var index = i;
                var file = pendingUploads[i];
                <div class="preview-item">
                    <button class="remove-media-btn" @onclick="() => RemoveFile(index)">×</button>
                    @if (file.MediaType == "Video")
                    {
                        <video class="preview-media" src="@file.SavedPath"></video>
                    }
                    else
                    {
                        <img class="preview-media" src="@file.SavedPath" alt="Preview" />
                    }
                </div>
            }
        </div>
        <div class="modal-actions">
            <button class="login-btn secondary" @onclick="ClosePostModal">Cancel</button>
            <button class="login-btn" @onclick="HandleCreatePost" disabled="@isUploading">@(isUploading ? "Uploading..." : "Post")</button>
        </div>
    </div>
</div>
}

@* Request Access Modal *@
@if (showRequestModal)
{
    <div class="modal">
        <div class="modal-content">
            <span class="modal-close" @onclick="() => showRequestModal = false">×</span>
            <h2>Request Access</h2>
            <div class="modal-subtitle">Requesting permission to post in verified category <b>#@CurrentCategory?.CategoryName</b></div>
            <textarea class="modal-textarea" placeholder="Why should you be verified for this category?" @bind="requestReason"></textarea>
            <div class="modal-actions">
                <button class="login-btn secondary" @onclick="() => showRequestModal = false">Cancel</button>
                <button class="login-btn" @onclick="HandleSubmitRequest">Submit Request</button>
            </div>
        </div>
    </div>
}

@* Report Modal *@
@if (showReportModal)
{
    <div class="modal">
        <div class="modal-content">
            <span class="modal-close" @onclick="() => showReportModal = false">×</span>
            <h2 class="modal-title-error">Report Content</h2>
            <div class="modal-subtitle">Help us keep the community safe. Why are you reporting this post?</div>
            <div class="form-group">
                <label>Reason:</label>
                <select class="modal-textarea form-select" @bind="reportReasonSelection">
                    <option value="Spam">Spam</option>
                    <option value="Harassment">Harassment or Bullying</option>
                    <option value="Misinformation">Misinformation</option>
                    <option value="Hate Speech">Hate Speech</option>
                    <option value="Inappropriate">Inappropriate Content</option>
                    <option value="Other">Other</option>
                </select>
            </div>
            <div class="form-group">
                <label>Additional Details (Optional):</label>
                <textarea class="modal-textarea" placeholder="Please provide more details..." @bind="reportReasonDetails"></textarea>
            </div>
            <div class="modal-actions">
                <button class="login-btn secondary" @onclick="() => showReportModal = false">Cancel</button>
                <button class="login-btn btn-error" @onclick="SubmitReport">Submit Report</button>
            </div>
        </div>
    </div>
}

@* Notification Modal *@
@if (showNotificationModal)
{
    <div class="modal">
        <div class="modal-content modal-notification">
            <span class="modal-close" @onclick="CloseNotification">×</span>
            <div class="notification-icon">
                @if (notificationType == "success")
                {
                    <i class="fas fa-check-circle success-icon"></i>
                }
                else if (notificationType == "info")
                {
                    <i class="fas fa-info-circle info-icon"></i>
                }
                else
                {
                    <i class="fas fa-exclamation-circle error-icon"></i>
                }
            </div>
            <h2 class="notification-title @notificationType">
                @(notificationType == "success" ? "Success" : notificationType == "info" ? "Report Received" : "Error")
            </h2>
            <p class="notification-message">@notificationMessage</p>
            <button class="login-btn" @onclick="CloseNotification" style="width: 100px;">OK</button>
        </div>
    </div>
}

@* Create Category Confirmation Modal *@
<div class="modal" style="display: @(showCreateConfirm ? "flex" : "none")">
    <div class="modal-content">
        <span class="modal-close" @onclick="() => showCreateConfirm = false">×</span>
        <h2>Category Not Found</h2>
        <p>Would you like to create <b>#@searchTerm</b>?</p>
        <div style="display: flex; gap: 10px; margin-top: 20px;">
            <button class="login-btn" style="flex:1;" @onclick="NavigateToCreate">Yes, Create</button>
            <button class="login-btn secondary" style="flex:1;" @onclick="() => showCreateConfirm = false">Cancel</button>
        </div>
    </div>
</div>

@* Auth Overlay *@
<div id="auth-overlay" class="auth-overlay" style="display: @(ShowLoginOverlay ? "flex" : "none");">
    <div class="auth-card">
        <div class="auth-card-header">
            <h3>Welcome</h3>
            <span style="cursor:pointer; font-size:20px;" @onclick="() => ShowLoginOverlay = false">×</span>
        </div>
        <div id="login-view">
            <form action="api/Account/login" method="post">
                <div class="auth-input-group">
                    <input type="text" name="username" placeholder="Username" required />
                </div>
                <div class="auth-input-group">
                    <input type="password" name="password" placeholder="Password" required />
                </div>
                <button type="submit" class="auth-btn">Log In</button>
            </form>
            <div class="auth-switch">Don't have an account? <a onclick="document.getElementById('login-view').style.display='none'; document.getElementById('register-view').style.display='block';">Register</a></div>
        </div>
        <div id="register-view" style="display: none;">
            <form action="api/Account/register" method="post">
                <div class="auth-input-group">
                    <input type="email" name="email" placeholder="Email Address" required />
                </div>
                <div class="auth-input-group">
                    <input type="text" name="username" placeholder="Username" required />
                </div>
                <div class="auth-input-group">
                    <input type="password" name="password" placeholder="Password" required />
                </div>
                <button type="submit" class="auth-btn">Sign Up</button>
            </form>
            <div class="auth-switch">Already have an account? <a onclick="document.getElementById('register-view').style.display='none'; document.getElementById('login-view').style.display='block';">Log In</a></div>
        </div>
    </div>
</div>

@code {
    [Parameter] public int Id { get; set; }

    // Reads focusPostId from the URL ?focusPostId=123
    [SupplyParameterFromQuery] public int? FocusPostId { get; set; }

    // ============ DATA MODELS ============
    private Categories? CurrentCategory;
    private List<Posts> CategoryPosts = new();
    private List<Categories> RecentCategories = new();
    private List<Categories> FollowedCategories = new();
    private List<Categories> VerifiedCategories = new();

    // ============ UI STATE ============
    private bool IsLoading = true;
    private string Title = "Category";
    private string errorMessage = "";
    private bool ShowLoginOverlay = false;

    // ============ USER STATE ============
    private string? currentUserId;
    private bool isAdmin = false;
    private Users? currentUser;

    // ============ CATEGORY STATE ============
    private bool isFollowing = false;
    private bool hasApprovedAccess = false;
    private bool hasPendingRequest = false;

    // ============ MODAL VISIBILITY ============
    private bool showPostModal = false;
    private bool showRequestModal = false;
    private bool showReportModal = false;
    private bool showNotificationModal = false;
    private bool showCreateConfirm = false;

    // ============ FORM DATA ============
    private string searchTerm = "";
    private string postSearchTerm = "";
    private string newPostContent = "";
    private string requestReason = "";
    private int reportPostId = 0;
    private string reportReasonSelection = "Spam";
    private string reportReasonDetails = "";
    private string notificationMessage = "";
    private string notificationType = "success";

    // ============ COMMENTS & MEDIA ============
    private Dictionary<int, bool> _showComments = new();
    private Dictionary<int, List<Comments>> _postComments = new();
    private Dictionary<int, string> _commentDrafts = new();
    private Dictionary<int, int> _carouselIndices = new();
    private List<PendingFile> pendingUploads = new();
    private bool isUploading = false;

    // ============ CONSTANTS ============
    private const long MaxFileSize = 1024 * 1024 * 200;
    private const int MaxAllowedFiles = 10;

    // ============ LIFECYCLE ============
    protected override async Task OnParametersSetAsync()
    {
        await InitializePageData();
    }

    // ============ INITIALIZATION ============
    private async Task InitializePageData()
    {
        ResetPageState();
        try
        {
            using var context = DbFactory.CreateDbContext();
            var authState = await AuthStateProvider.GetAuthenticationStateAsync();
            var user = authState.User;

            await LoadUserData(user, context);
            await LoadCategoryData(context);
            await LoadUserSpecificData(context);
        }
        catch (Exception ex)
        {
            errorMessage = "Error loading: " + ex.Message;
        }
        finally
        {
            IsLoading = false;
        }
    }

    private void ResetPageState()
    {
        IsLoading = true;
        errorMessage = "";
        hasApprovedAccess = false;
        hasPendingRequest = false;
    }

    private async Task LoadUserData(System.Security.Claims.ClaimsPrincipal user, SoSuSaFsdContext context)
    {
        currentUserId = user.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)?.Value;

        if (currentUserId == null && user.Identity?.IsAuthenticated == true)
        {
            using var scope = ScopeFactory.CreateScope();
            var userManager = scope.ServiceProvider.GetRequiredService<UserManager<Users>>();
            var dbUser = await userManager.GetUserAsync(user);
            currentUserId = dbUser?.Id;
            currentUser = dbUser;
        }
        else if (currentUserId != null)
        {
            using var scope = ScopeFactory.CreateScope();
            var userManager = scope.ServiceProvider.GetRequiredService<UserManager<Users>>();
            currentUser = await userManager.FindByIdAsync(currentUserId);
        }

        if (user.Identity?.IsAuthenticated == true)
        {
            isAdmin = user.IsInRole("Admin");
        }
    }

    private async Task LoadCategoryData(SoSuSaFsdContext context)
    {
        VerifiedCategories = await context.Categories
            .Where(c => c.IsVerified == true)
            .OrderByDescending(c => c.DateCreated)
            .Take(5)
            .ToListAsync();

        CurrentCategory = await context.Categories.FirstOrDefaultAsync(c => c.Id == Id);
        if (CurrentCategory != null)
        {
            Title = "#" + CurrentCategory.CategoryName;
        }

        CategoryPosts = await context.Posts
            .Where(p => p.CategoryId == Id)
            .Include(p => p.User)
            .Include(p => p.Media)
            .Include(p => p.Likes)
            .OrderByDescending(p => p.DateCreated)
            .ToListAsync();
    }

    private async Task LoadUserSpecificData(SoSuSaFsdContext context)
    {
        if (string.IsNullOrEmpty(currentUserId) || CurrentCategory == null)
        {
            return;
        }

        var existingFollow = await context.CategoryFollows
            .FirstOrDefaultAsync(cf => cf.UserId == currentUserId && cf.CategoryId == CurrentCategory.Id);
        isFollowing = (existingFollow != null);

        var accessRequest = await context.CategoryAccessRequests
            .OrderByDescending(r => r.DateCreated)
            .FirstOrDefaultAsync(r => r.UserId == currentUserId && r.CategoryId == CurrentCategory.Id);

        if (accessRequest != null)
        {
            hasApprovedAccess = accessRequest.Status == "Approved";
            hasPendingRequest = accessRequest.Status == "Pending";
        }

        RecentCategories = await context.Categories
            .Where(c => c.CreatedBy == currentUserId)
            .OrderByDescending(c => c.DateCreated)
            .Take(5)
            .ToListAsync();

        FollowedCategories = await context.CategoryFollows
            .Where(cf => cf.UserId == currentUserId)
            .Include(cf => cf.Category)
            .Select(cf => cf.Category!)
            .ToListAsync();
    }

    // ========== ADDED METHOD TO FIX RZ1010 ==========
    private void ClearSearchAndNavigate()
    {
        FocusPostId = null;
        Navigation.NavigateTo($"/category/{Id}");
    }

    // ========== NEW REFRESH METHOD ==========
    private void RefreshPage()
    {
        Navigation.NavigateTo(Navigation.Uri, forceLoad: true);
    }

    private int GetCurrentIndex(int postId)
    {
        if (!_carouselIndices.ContainsKey(postId))
        {
            _carouselIndices[postId] = 0;
        }
        return _carouselIndices[postId];
    }

    private void NextSlide(int postId, int totalCount)
    {
        var currentIndex = GetCurrentIndex(postId);
        _carouselIndices[postId] = (currentIndex + 1) % totalCount;
    }

    private void PrevSlide(int postId, int totalCount)
    {
        var currentIndex = GetCurrentIndex(postId);
        _carouselIndices[postId] = (currentIndex - 1 + totalCount) % totalCount;
    }

    private bool IsCommentSectionVisible(int postId)
    {
        return _showComments.ContainsKey(postId) && _showComments[postId];
    }

    private async Task ToggleComments(int postId)
    {
        if (string.IsNullOrEmpty(currentUserId))
        {
            ShowLoginPopup("comment on posts");
            return;
        }

        if (!_showComments.ContainsKey(postId))
        {
            _showComments[postId] = false;
        }

        _showComments[postId] = !_showComments[postId];

        if (_showComments[postId])
        {
            await LoadComments(postId);
        }

        StateHasChanged();
    }

    private async Task LoadComments(int postId)
    {
        if (!_postComments.ContainsKey(postId) || _postComments[postId] == null)
        {
            try
            {
                using var context = DbFactory.CreateDbContext();
                var comments = await context.Comments
                    .Where(c => c.PostID == postId)
                    .Include(c => c.User)
                    .OrderBy(c => c.DateCreated)
                    .ToListAsync();
                _postComments[postId] = comments;
            }
            catch (Exception ex)
            {
                Console.WriteLine("Error loading comments: " + ex.Message);
            }
        }
    }

    private void UpdateCommentDraft(int postId, string? value)
    {
        if (value != null)
        {
            _commentDrafts[postId] = value;
        }
    }

    private async Task SubmitComment(int postId)
    {
        if (currentUser == null)
        {
            ShowLoginPopup("post comments");
            return;
        }

        if (!_commentDrafts.ContainsKey(postId) || string.IsNullOrWhiteSpace(_commentDrafts[postId]))
        {
            return;
        }

        var content = _commentDrafts[postId];

        try
        {
            using var context = DbFactory.CreateDbContext();
            var newComment = new Comments
            {
                PostID = postId,
                UserID = currentUser.Id,
                Content = content,
                DateCreated = DateTime.Now,
                DateUpdated = DateTime.Now,
                CreatedBy = currentUser.Id,
                UpdatedBy = currentUser.Id
            };

            context.Comments.Add(newComment);
            await context.SaveChangesAsync();

            _commentDrafts[postId] = "";
            newComment.User = currentUser;

            if (!_postComments.ContainsKey(postId))
            {
                _postComments[postId] = new List<Comments>();
            }

            _postComments[postId].Add(newComment);
            StateHasChanged();
        }
        catch (Exception ex)
        {
            Console.WriteLine("Error posting comment: " + ex.Message);
        }
    }

    private async Task ToggleLike(int postId)
    {
        if (string.IsNullOrEmpty(currentUserId))
        {
            ShowLoginPopup("like posts");
            return;
        }

        try
        {
            using var context = DbFactory.CreateDbContext();
            var existingLike = await context.PostLikes
                .FirstOrDefaultAsync(l => l.PostID == postId && l.UserID == currentUserId);
            var post = CategoryPosts.FirstOrDefault(p => p.Id == postId);

            if (existingLike != null)
            {
                context.PostLikes.Remove(existingLike);
                if (post != null)
                {
                    var localLike = post.Likes.FirstOrDefault(l => l.UserID == currentUserId);
                    if (localLike != null)
                    {
                        post.Likes.Remove(localLike);
                    }
                }
            }
            else
            {
                var newLike = new PostLikes
                {
                    PostID = postId,
                    UserID = currentUserId,
                    LikedAt = DateTime.Now
                };
                context.PostLikes.Add(newLike);
                if (post != null)
                {
                    post.Likes.Add(newLike);
                }
            }

            await context.SaveChangesAsync();
            StateHasChanged();
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error toggling like: {ex.Message}");
        }
    }

    private bool CanUserPost()
    {
        if (CurrentCategory?.IsVerified == true)
        {
            return isAdmin || hasApprovedAccess;
        }
        return !string.IsNullOrEmpty(currentUserId);
    }

    private async Task HandleCreatePost()
    {
        if (string.IsNullOrWhiteSpace(newPostContent) && pendingUploads.Count == 0)
        {
            return;
        }

        if (string.IsNullOrEmpty(currentUserId))
        {
            ShowLoginPopup("create posts");
            return;
        }

        if (CurrentCategory?.IsVerified == true && !isAdmin && !hasApprovedAccess)
        {
            errorMessage = "This category is verified. You must request access to post here.";
            return;
        }

        try
        {
            using var context = DbFactory.CreateDbContext();
            bool dbHasAccess = await context.CategoryAccessRequests
                .AnyAsync(r => r.UserId == currentUserId && r.CategoryId == Id && r.Status == "Approved");

            if (CurrentCategory?.IsVerified == true && !isAdmin && !dbHasAccess)
            {
                errorMessage = "This category is verified. You must request access to post here.";
                return;
            }

            var newPost = new Posts
            {
                Content = newPostContent,
                CategoryId = Id,
                UserId = currentUserId,
                PostStatus = "Published",
                DateCreated = DateTime.Now,
                DateUpdated = DateTime.Now,
                CreatedBy = currentUserId,
                UpdatedBy = currentUserId,
                Media = new List<PostMedia>()
            };

            foreach (var file in pendingUploads)
            {
                newPost.Media.Add(new PostMedia
                {
                    MediaPath = file.SavedPath,
                    MediaType = file.MediaType
                });
            }

            context.Posts.Add(newPost);
            await context.SaveChangesAsync();

            ClosePostModal();

            CategoryPosts = await context.Posts
                .Where(p => p.CategoryId == Id)
                .Include(p => p.User)
                .Include(p => p.Media)
                .Include(p => p.Likes)
                .OrderByDescending(p => p.DateCreated)
                .ToListAsync();
        }
        catch (Exception ex)
        {
            errorMessage = "Error posting: " + ex.Message;
        }
    }

    private void ClosePostModal()
    {
        showPostModal = false;
        newPostContent = "";
        pendingUploads.Clear();
        errorMessage = "";
    }

    private async Task HandleFilesSelected(InputFileChangeEventArgs e)
    {
        errorMessage = "";
        isUploading = true;

        try
        {
            var newFiles = e.GetMultipleFiles(MaxAllowedFiles);
            var uploadsFolder = Path.Combine(Environment.WebRootPath, "uploads");

            if (!Directory.Exists(uploadsFolder))
            {
                Directory.CreateDirectory(uploadsFolder);
            }

            foreach (var file in newFiles)
            {
                if (pendingUploads.Count >= MaxAllowedFiles)
                {
                    break;
                }

                if (file.Size <= MaxFileSize)
                {
                    try
                    {
                        var extension = Path.GetExtension(file.Name).ToLower();
                        bool isVideo = (extension == ".mp4" || extension == ".webm" || extension == ".mov");
                        var fileName = $"{Guid.NewGuid()}{extension}";
                        var physicalPath = Path.Combine(uploadsFolder, fileName);
                        var webPath = $"/uploads/{fileName}";

                        await using var fileStream = new FileStream(physicalPath, FileMode.Create);
                        await file.OpenReadStream(MaxFileSize).CopyToAsync(fileStream);

                        pendingUploads.Add(new PendingFile
                        {
                            SavedPath = webPath,
                            PhysicalPath = physicalPath,
                            MediaType = isVideo ? "Video" : "Image"
                        });
                    }
                    catch (Exception ex)
                    {
                        errorMessage = $"Error reading {file.Name}: {ex.Message}";
                    }
                }
                else
                {
                    errorMessage = $"File {file.Name} is too large.";
                }
            }
        }
        finally
        {
            isUploading = false;
        }
    }

    private void RemoveFile(int index)
    {
        if (index >= 0 && index < pendingUploads.Count)
        {
            var file = pendingUploads[index];
            try
            {
                if (System.IO.File.Exists(file.PhysicalPath))
                {
                    System.IO.File.Delete(file.PhysicalPath);
                }
            }
            catch { }
            pendingUploads.RemoveAt(index);
        }
    }

    private async Task ToggleFollow()
    {
        if (string.IsNullOrEmpty(currentUserId))
        {
            ShowLoginPopup("follow categories");
            return;
        }

        try
        {
            using var context = DbFactory.CreateDbContext();

            if (isFollowing)
            {
                var record = await context.CategoryFollows
                    .FirstOrDefaultAsync(cf => cf.UserId == currentUserId && cf.CategoryId == Id);
                if (record != null)
                {
                    context.CategoryFollows.Remove(record);
                }
                isFollowing = false;
            }
            else
            {
                context.CategoryFollows.Add(new CategoryFollows
                {
                    UserId = currentUserId,
                    CategoryId = Id,
                    DateCreated = DateTime.Now,
                    DateUpdated = DateTime.Now
                });
                isFollowing = true;
            }

            await context.SaveChangesAsync();

            FollowedCategories = await context.CategoryFollows
                .Where(cf => cf.UserId == currentUserId)
                .Include(cf => cf.Category)
                .Select(cf => cf.Category!)
                .ToListAsync();
        }
        catch (Exception ex)
        {
            errorMessage = ex.Message;
        }
    }

    private async Task HandleSubmitRequest()
    {
        if (string.IsNullOrEmpty(currentUserId))
        {
            ShowLoginPopup("request category access");
            return;
        }

        try
        {
            using var context = DbFactory.CreateDbContext();
            var exists = await context.CategoryAccessRequests
                .AnyAsync(r => r.UserId == currentUserId && r.CategoryId == Id && r.Status == "Pending");

            if (exists)
            {
                errorMessage = "You already have a pending request.";
                showRequestModal = false;
                hasPendingRequest = true;
                return;
            }

            var request = new CategoryAccessRequests
            {
                UserId = currentUserId,
                CategoryId = Id,
                Reason = requestReason,
                Status = "Pending",
                DateCreated = DateTime.Now,
                DateUpdated = DateTime.Now
            };

            context.CategoryAccessRequests.Add(request);
            await context.SaveChangesAsync();

            showRequestModal = false;
            requestReason = "";
            hasPendingRequest = true;
            StateHasChanged();
        }
        catch (Exception ex)
        {
            errorMessage = "Error sending request: " + ex.Message;
        }
    }

    private void OpenReportModal(int postId)
    {
        if (string.IsNullOrEmpty(currentUserId))
        {
            ShowLoginPopup("report posts");
            return;
        }

        reportPostId = postId;
        reportReasonSelection = "Spam";
        reportReasonDetails = "";
        showReportModal = true;
    }

    private async Task SubmitReport()
    {
        if (reportPostId == 0 || string.IsNullOrEmpty(currentUserId))
        {
            ShowNotification("Session error. Please refresh and try again.", "error");
            return;
        }

        try
        {
            using var context = DbFactory.CreateDbContext();
            var existingReport = await context.Reports
                .FirstOrDefaultAsync(r => r.PostID == reportPostId && r.ReporterID == currentUserId);

            if (existingReport != null)
            {
                showReportModal = false;
                if (existingReport.Status == "Dismissed")
                {
                    ShowNotification("An Admin has already dismissed your report for this post.", "error");
                }
                else
                {
                    ShowNotification("You have already reported this post.", "error");
                }
                return;
            }

            bool isAlreadyDismissed = await context.Reports
                .AnyAsync(r => r.PostID == reportPostId && r.Status == "Dismissed");

            string finalReason = reportReasonSelection;
            if (!string.IsNullOrWhiteSpace(reportReasonDetails))
            {
                finalReason = $"{reportReasonSelection}: {reportReasonDetails}";
            }

            var newReport = new Reports
            {
                PostID = reportPostId,
                ReporterID = currentUserId,
                Reason = finalReason,
                Status = "Pending",
                DateCreated = DateTime.Now
            };

            context.Reports.Add(newReport);
            await context.SaveChangesAsync();

            showReportModal = false;

            if (isAlreadyDismissed)
            {
                ShowNotification("Report submitted, but this content has already been reviewed and dismissed by admins.", "info");
            }
            else
            {
                ShowNotification("Report submitted successfully.", "success");
            }
        }
        catch (Exception ex)
        {
            ShowNotification("Error sending report: " + ex.Message, "error");
        }
    }

    private void ShowNotification(string message, string type)
    {
        notificationMessage = message;
        notificationType = type;
        showNotificationModal = true;
    }

    private void CloseNotification()
    {
        showNotificationModal = false;
        notificationMessage = "";
    }

    private void NavigateToCategory(int id) => Navigation.NavigateTo($"/category/{id}", forceLoad: true);

    private async Task HandleCategorySearch()
    {
        if (string.IsNullOrWhiteSpace(searchTerm))
        {
            return;
        }

        using var context = DbFactory.CreateDbContext();
        var cat = await context.Categories
            .FirstOrDefaultAsync(c => c.CategoryName.ToLower() == searchTerm.ToLower());

        if (cat != null)
        {
            Navigation.NavigateTo($"/category/{cat.Id}", true);
        }
        else
        {
            showCreateConfirm = true;
        }
    }

    private void NavigateToCreate() => Navigation.NavigateTo($"/categories/create?name={searchTerm}");

    private void ShowLoginPopup(string action) => ShowLoginOverlay = true;

    // ============ HELPER METHODS ============
    private RenderFragment RenderCarousel(Posts post)
    {
        return builder =>
        {
            var mediaList = post.Media.ToList();
            var totalMedia = mediaList.Count;
            var currentIndex = GetCurrentIndex(post.Id);
            var activeMedia = mediaList[currentIndex];

            builder.OpenElement(0, "div");
            builder.AddAttribute(1, "class", "carousel-container");

            if (totalMedia > 1)
            {
                builder.OpenElement(2, "button");
                builder.AddAttribute(3, "class", "carousel-btn prev");
                // Correct attribute syntax for RenderTreeBuilder
                builder.AddAttribute(4, "onclick", EventCallback.Factory.Create(this, () => PrevSlide(post.Id, totalMedia)));
                builder.AddContent(5, "❮");
                builder.CloseElement();
            }

            if (activeMedia.MediaType == "Video")
            {
                builder.OpenElement(6, "video");
                builder.AddAttribute(7, "class", "carousel-media");
                builder.AddAttribute(8, "controls", true);
                builder.AddAttribute(9, "src", activeMedia.MediaPath);
                builder.AddAttribute(10, "key", activeMedia.MediaPath);
                builder.CloseElement();
            }
            else
            {
                builder.OpenElement(11, "img");
                builder.AddAttribute(12, "class", "carousel-media");
                builder.AddAttribute(13, "src", activeMedia.MediaPath);
                builder.AddAttribute(14, "alt", "Post Media");
                builder.AddAttribute(15, "key", activeMedia.MediaPath);
                builder.CloseElement();
            }

            if (totalMedia > 1)
            {
                builder.OpenElement(16, "button");
                builder.AddAttribute(17, "class", "carousel-btn next");
                builder.AddAttribute(18, "onclick", EventCallback.Factory.Create(this, () => NextSlide(post.Id, totalMedia)));
                builder.AddContent(19, "❯");
                builder.CloseElement();

                builder.OpenElement(20, "div");
                builder.AddAttribute(21, "class", "carousel-counter");
                builder.AddContent(22, $"{currentIndex + 1} / {totalMedia}");
                builder.CloseElement();
            }

            builder.CloseElement();
        };
    }

    private RenderFragment RenderLikeButton(Posts post)
    {
        return builder =>
        {
            var isLiked = post.Likes.Any(l => l.UserID == currentUserId);
            var likeCount = post.Likes.Count;

            if (isLiked)
            {
                builder.OpenElement(0, "i");
                builder.AddAttribute(1, "class", "fas fa-heart");
                builder.AddAttribute(2, "style", "color: #e0245e; margin-right: 5px;");
                builder.CloseElement();
            }
            else
            {
                builder.OpenElement(3, "i");
                builder.AddAttribute(4, "class", "far fa-heart");
                builder.AddAttribute(5, "style", "margin-right: 5px;");
                builder.CloseElement();
            }

            builder.OpenElement(6, "span");
            if (likeCount > 0)
            {
                builder.AddContent(7, likeCount);
            }
            else
            {
                builder.AddContent(7, "Like");
            }
            builder.CloseElement();
        };
    }

    private RenderFragment RenderCommentSection(Posts post)
    {
        return builder =>
        {
            builder.OpenElement(0, "div");
            builder.AddAttribute(1, "class", "comment-section");

            builder.OpenElement(2, "div");
            builder.AddAttribute(3, "class", "comment-list");

            if (_postComments.ContainsKey(post.Id) && _postComments[post.Id] != null)
            {
                foreach (var comment in _postComments[post.Id])
                {
                    builder.OpenElement(4, "div");
                    builder.AddAttribute(5, "class", "comment-item");

                    builder.OpenElement(6, "div");
                    builder.AddAttribute(7, "class", "comment-avatar");
                    builder.AddAttribute(8, "style", $"background-image: url('{comment.User?.ProfileImage ?? ""}');");
                    builder.CloseElement();

                    builder.OpenElement(9, "div");
                    builder.AddAttribute(10, "class", "comment-bubble");

                    builder.OpenElement(11, "div");
                    builder.OpenElement(12, "span");
                    builder.AddAttribute(13, "class", "comment-user");
                    builder.AddContent(14, comment.User?.UserName ?? "User");
                    builder.CloseElement();
                    builder.OpenElement(15, "span");
                    builder.AddAttribute(16, "class", "comment-date");
                    builder.AddContent(17, comment.DateCreated.ToString("MMM dd, HH:mm"));
                    builder.CloseElement();
                    builder.CloseElement();

                    builder.OpenElement(18, "div");
                    builder.AddAttribute(19, "class", "comment-text");
                    builder.AddContent(20, comment.Content);
                    builder.CloseElement();

                    builder.CloseElement();
                    builder.CloseElement();
                }
            }

            builder.CloseElement();

            builder.OpenElement(21, "div");
            builder.AddAttribute(22, "class", "comment-input-area");

            builder.OpenElement(23, "input");
            builder.AddAttribute(24, "type", "text");
            builder.AddAttribute(25, "class", "comment-input");
            builder.AddAttribute(26, "placeholder", "Write a comment...");
            builder.AddAttribute(27, "value", _commentDrafts.ContainsKey(post.Id) ? _commentDrafts[post.Id] : "");

            // Correct attribute syntax for RenderTreeBuilder
            builder.AddAttribute(28, "onchange", EventCallback.Factory.Create(this, (ChangeEventArgs e) => UpdateCommentDraft(post.Id, e.Value?.ToString())));

            builder.CloseElement();

            builder.OpenElement(29, "button");
            builder.AddAttribute(30, "class", "comment-send-btn");
            // Correct attribute syntax for RenderTreeBuilder
            builder.AddAttribute(31, "onclick", EventCallback.Factory.Create(this, () => SubmitComment(post.Id)));
            builder.AddContent(32, "Post");
            builder.CloseElement();

            builder.CloseElement();
            builder.CloseElement();
        };
    }

    // ============ HELPER CLASSES ============
    private class PendingFile
    {
        public string SavedPath { get; set; } = "";
        public string MediaType { get; set; } = "";
        public string PhysicalPath { get; set; } = "";
    }
}